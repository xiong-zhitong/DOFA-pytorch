defaults:
  - base_model

model_type: satmae
out_features: true
task: classification
freeze_backbone: true
embed_dim: 1024
image_resolution: 224
patch_size: 16
num_channels: 3
pretrained_path: "checkpoint_ViT-L_pretrain_fmow_rgb.pth"

